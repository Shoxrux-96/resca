kind = "web"
previewPath = "/"
title = "Restoran & Kafe POS/CRM"
version = "1.0.0"
id = "artifacts/pos-crm"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24899

[services.development]
run = "pnpm --filter @workspace/pos-crm run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pos-crm", "run", "build" ]
publicDir = "artifacts/pos-crm/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24899"
BASE_PATH = "/"
